export enum PAGE_ROUTES {
    HOME = "/home",
    REGISTER = "/register",
    LOGIN = "/login",
    NOT_FOUND = "*"
}


export enum PAGE_STATES {
    LOGIN = "login",
    REGISTRATION = "registration"
}

export enum API_ROUTES {
    LOGIN = "/api/v1/login",
    DATA = "/api/v1/data"

}