export enum RouterNames {
    HOME = "/home",
    REGISTER = "/register",
    LOGIN = "/login",
    NOT_FOUND = "*"
}


export enum PageStates {
    LOGIN = "login",
    REGISTRATION = "registration"
}